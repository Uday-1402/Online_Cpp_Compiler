apiVersion: v1
kind: Service
metadata:
  name: online-cpp-compiler-service
  labels:
    app: cpp-compiler-app
spec:
  type: NodePort
  selector:
    app: cpp-compiler-app
  ports:
    - port: 80
      targetPort: 3000
      nodePort: 30001